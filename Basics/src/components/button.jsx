import React from 'react'

const button = (props) => {
    
  return (
    <div>
      <h1 className=' rounded text-center font-semibold text-xl bg-red-800'>Me hu jiyan {props.user}</h1>
    </div>
  )
}

export default button
